---
phase: 02-text-to-image
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  Amuse.API/Program.cs,
  Amuse.API/Models/Text2ImgRequest.cs,
  Amuse.API/Services/StableDiffusionService.cs
]
autonomous: true
requirements: [T2I-01, T2I-02, T2I-03, T2I-04, T2I-05, T2I-06]
user_setup: []

must_haves:
  truths:
    - "Users can generate images from text prompts via POST /api/generate/text2img"
    - "Negative prompt parameter influences generation output"
    - "Width and height parameters accept values between 512-1024 pixels"
    - "Steps and guidance_scale parameters affect generation quality and style"
    - "Seed parameter produces reproducible results when reused"
    - "Generated images are returned as Base64 strings in JSON response"
  artifacts:
    - path: "Amuse.API/Program.cs"
      provides: "Text-to-image API endpoint"
      exports: ["POST /api/generate/text2img"]
    - path: "Amuse.API/Models/Text2ImgRequest.cs"
      provides: "Request validation model"
      contains: "public class Text2ImgRequest"
    - path: "Amuse.API/Services/StableDiffusionService.cs"
      provides: "Stable Diffusion generation logic"
      exports: ["GenerateAsync"]
    - path: "Amuse.API/appsettings.json"
      provides: "Model configuration"
      contains: "StableDiffusionModelPath"
  key_links:
    - from: "Amuse.API/Program.cs"
      to: "Amuse.API/Models/Text2ImgRequest.cs"
      via: "parameter binding"
      pattern: "Text2ImgRequest request"
    - from: "Amuse.API/Program.cs"
      to: "Amuse.API/Services/StableDiffusionService.cs"
      via: "dependency injection"
      pattern: "StableDiffusionService"
    - from: "Amuse.API/Services/StableDiffusionService.cs"
      to: "Amuse.UI.Services.ModelFactory"
      via: "model loading pattern"
      pattern: "ModelFactory.CreateStableDiffusionPipeline"

<tasks>
<task type="auto">
  <name>Task 1: Create text-to-image request model with validation</name>
  <files>Amuse.API/Models/Text2ImgRequest.cs</files>
  <action>
    Create a C# model class for text-to-image generation requests with properties for:
    - Prompt (required string)
    - NegativePrompt (optional string)
    - Width (int, range 512-1024, default 512)
    - Height (int, range 512-1024, default 512)
    - Steps (int, range 1-100, default 20)
    - GuidanceScale (float, range 0.0-20.0, default 7.5)
    - Seed (int?, optional for reproducibility)
    Add data annotation validation for ranges and requirements.
    Follow existing patterns in Amuse.UI for parameter validation.
  </action>
  <verify>
    dotnet build Amuse.API
  </verify>
  <done>
    Model class compiles successfully with proper validation attributes
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Stable Diffusion service for image generation</name>
  <files>Amuse.API/Services/StableDiffusionService.cs</files>
  <action>
    Create a service class that handles Stable Diffusion image generation:
    - Inject IConfiguration for accessing model paths
    - Implement GenerateAsync method that takes Text2ImgRequest
    - Use OnnxStack.StableDiffusion pipeline (reference Amuse.UI patterns)
    - Convert parameters from request to pipeline options
    - Generate image and convert to Base64 string
    - Handle errors with proper logging
    - Return Base64 string of generated PNG image
    Follow the same patterns used in Amuse.UI.Views.StableDiffusion.TextToImageView
  </action>
  <verify>
    dotnet build Amuse.API
  </verify>
  <done>
    Service class compiles successfully and implements GenerateAsync method
  </done>
</task>

<task type="auto">
  <name>Task 3: Add text-to-image endpoint to API</name>
  <files>Amuse.API/Program.cs</files>
  <action>
    Add a POST endpoint for text-to-image generation:
    - Map POST /api/generate/text2img
    - Accept Text2ImgRequest as JSON body
    - Validate model state and return 400 for invalid requests
    - Inject StableDiffusionService via dependency injection
    - Call service to generate image and return Base64 result
    - Return 200 OK with JSON: { image: "base64string" }
    - Handle exceptions and return 500 with error details
    - Follow same pattern as existing /health and /api/info endpoints
    - Add Swagger/OpenAPI documentation comments
  </action>
  <verify>
    dotnet build Amuse.API
  </verify>
  <done>
    Endpoint compiles successfully and is registered in the application pipeline
  </done>
</task>
</tasks>

<verification>
Verify all requirements are met by checking:
- Endpoint responds to POST /api/generate/text2img
- Validation works for parameter ranges
- Image generation returns Base64 string
- All parameters (prompt, negative prompt, dimensions, steps, guidance, seed) are handled
</verification>

<success_criteria>
All T2I requirements satisfied:
- T2I-01: POST /api/generate/text2img endpoint exists and functions
- T2I-02: Negative prompt parameter influences generation
- T2I-03: Width/height parameters work in 512-1024 range
- T2I-04: Steps and guidance_scale affect generation
- T2I-05: Seed parameter produces reproducible results
- T2I-06: Output returned as Base64 string in JSON response
</success_criteria>

<output>
After completion, create .planning/phases/02-text-to-image/02-01-SUMMARY.md
</output>