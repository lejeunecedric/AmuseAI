<local:BaseDialog x:Class="Amuse.UI.Dialogs.EZMode.InformationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Amuse.UI.Dialogs"
        xmlns:controls="clr-namespace:Amuse.UI.UserControls"
        mc:Ignorable="d"
        x:Name="UI"
        Icon="/Images/Icon.png"
        MinWidth="500"
        MaxWidth="500"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner" 
        SnapsToDevicePixels="True"
        AllowsTransparency="True"
        WindowStyle="None"
        Title="Information"
        Style="{StaticResource BaseToolWindow}">
    <DockPanel DataContext="{Binding ElementName=UI}" Margin="15, 5, 15, 10">
        <DockPanel DockPanel.Dock="Bottom" Margin="0,0,0,0" >
            <UniformGrid DockPanel.Dock="Right" Columns="2" Height="30" VerticalAlignment="Bottom">
                <TextBlock />
                <Button Content="Ok" Command="{Binding CancelCommand}" IsDefault="True" Width="100" Height="30"/>
            </UniformGrid>

            <StackPanel DockPanel.Dock="Left" Margin="2,0,0,0">
                <TextBlock Text="Copyright © "  Opacity=".6"/>
                <TextBlock Text="TensorStack Limited - 2026"  Opacity=".6"/>
            </StackPanel>
        </DockPanel>

        <DockPanel>
            <DockPanel DockPanel.Dock="Left" >
                <Image Source="{StaticResource ImageTensorStackSmall}" Width="140" Margin="0,2,10,5" RenderOptions.BitmapScalingMode="Fant" Opacity=".9"/>
            </DockPanel>
            <StackPanel>
                <TextBlock  FontSize="20" FontWeight="DemiBold" TextAlignment="Center" >
                    <Run Text="Amuse" />
                    <Run Text="{Binding Version, Mode=OneWay}" />
                    <Run Text="" FontSize="15" FontStyle="Italic" FontWeight="Regular" />
                </TextBlock>
                <Border BorderBrush="{StaticResource ControlDefaultForeground}" BorderThickness="0,0,0,1" Opacity=".5"/>
                <TextBlock Text="Amuse simplifies image creation with a broad selection of Stable Diffusion models. Efficient and user-friendly, Amuse caters to both enthusiasts and professionals, allowing for the easy production of high-quality images." TextWrapping="Wrap" TextAlignment="Center" />
                <UniformGrid Columns="2" HorizontalAlignment="Center">
                    <controls:HyperlinkControl Link="https://www.tensorstack.ai/" Label="www.tensorstack.ai" Margin="20,10"/>
                </UniformGrid>
            </StackPanel>
        </DockPanel>

    </DockPanel>
</local:BaseDialog>